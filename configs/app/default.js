var path = require('path');

module.exports = {

    // Root project path
    rootPath : path.resolve('./'),

    // Page
    page : {
        encoding  : 'utf-8',
        urlSuffix : 'html'
    }
};
